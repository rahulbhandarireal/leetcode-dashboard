spring:
  application:
    name: leetcode-battle-module

  datasource:
    url: jdbc:mysql://localhost:3306/leetcodedashboardBattle?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
      open-in-view: false

  data:
    redis:
      host: redis-16937.c90.us-east-1-3.ec2.cloud.redislabs.com
      port: 16937
      username: default
      password: diqsILCdxoqoxU6FRCLdiMpLcMTjUc3v
      ssl:
        enabled: false   # set true if your provider requires TLS (e.g. Upstash usually does)
      timeout: 5000ms
      connect-timeout: 5000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

server:
  port: 8082

ai-service:
  base-url: http://localhost:8085